type Result<T> = Result<T, Box<dyn std::error::Error>>;

fn main() {
    // init

    loop {
        // 각 취업정보 사이트 작업

        // 일정 시간동안 대기
    }
}

fn saramin() -> Result<()> {
    // 데이터 가져오기

    // 데이처 처리

    // 다음 호출에서 사용할 수 있도록
    // 마지막 데이터 저장

    Ok(())
}

fn jobkorea() -> Result<()> {
    // 데이터 가져오기

    // 데이터 처리

    // 다음 호출에서 사용할 수 있도록
    // 마지막 데이터 저장

    Ok(())
}

fn incruit() -> Result<()> {
    // 데이터 가져오기

    // 데이터 처리

    // 다음 호출에서 사용할 수 있도록
    // 마지막 데이터 저장

    Ok(())
}
